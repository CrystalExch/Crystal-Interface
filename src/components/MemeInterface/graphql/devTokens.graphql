query ($d: Bytes!, $skip: Int!, $first: Int!) {
  launchpadTokens(
    where: { creator: $d }
    orderBy: timestamp
    orderDirection: desc
    skip: $skip
    first: $first
  ) {
    id
    name
    symbol
    metadataCID
    lastPriceNativePerTokenWad
    timestamp
    migrated
  }
}
